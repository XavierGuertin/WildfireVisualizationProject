{
  "name": "frontend-e2e",
  "root": "apps/frontend-e2e",
  "sourceRoot": "apps/frontend-e2e/src",
  "projectType": "application",
  "targets": {
    "e2e": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/frontend-e2e",
        "command": "concurrently -k -s first \"npx nx dev frontend --configuration=coverage\" \"npx wait-on http://localhost:3000 && npx cypress run \""
      }
    },
    "e2e-open": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/frontend-e2e",
        "command": "concurrently -k -s first \"npx nx dev frontend\" \"npx nx dev backend\" \"npx wait-on http://localhost:3000 && npx cypress open\""
      }
    }
  },
  "tags": []
}