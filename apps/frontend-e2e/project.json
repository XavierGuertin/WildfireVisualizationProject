{
  "name": "frontend-e2e",
  "root": "apps/frontend-e2e",
  "sourceRoot": "apps/frontend-e2e/src",
  "projectType": "application",
  "targets": {
    "e2e": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "./",
        "command": "concurrently -k -s first \"npx nx serve frontend --configuration=coverage\" \"npx wait-on http://localhost:3000 && npx cypress run --config-file apps/frontend-e2e/cypress.config.ts\""
      }
    },
    "e2e-open": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/frontend-e2e",
        "command": "concurrently -k -s first \"npx nx serve frontend\" \"npx wait-on http://localhost:3000 && npx cypress open\""
      }
    }
  },
  "tags": []
}